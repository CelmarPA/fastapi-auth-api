# Generate strong SECRET_KEY (e.g.: python -c "import secrets; print(secrets.token_urlsafe(64))").
# ===== APP CONFIG =====
ENVIRONMENT=development

# ===== DATABASE =====
DATABASE_URL=sqlite:///./test.db

# ===== JWT CONFIG =====
# Generate strong SECRET_KEY (run: python -c "import secrets; print(secrets.token_urlsafe(64))").
SECRET_KEY=CHANGE_ME
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ===== CORS =====
CORS_ORIGINS='["http://localhost:3000", "http://127.0.0.1:3000"]'

# ===== Brevo API =====
BREVO_API_KEY=<YOUR_API_KEY_BREVO>
EMAIL_FROM=<YOUR_EMAIL>
EMAIL_FROM_NAME=<NAME>
